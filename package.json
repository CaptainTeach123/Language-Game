{
  "name": "word-wizard",
  "version": "1.0.0",
  "private": true,
  "description": "Word Wizard: a listening game that helps Anthony understand 50 first words. Real photos, a recorded voice, installs on iPhone from Safari.",
  "scripts": {
    "start": "npx --yes http-server -c-1 -p 8080 .",
    "test": "node --test tests/*.test.js",
    "build": "node tools/build-sw.js",
    "icons": "NODE_PATH=$(npm root -g) node tools/make-icons.js",
    "e2e": "NODE_PATH=$(npm root -g) node tools/e2e.js"
  }
}
