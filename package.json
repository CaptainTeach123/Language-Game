{
  "name": "word-buddies",
  "version": "1.0.0",
  "private": true,
  "description": "A talking picture game that helps toddlers with speech delay learn 50 first words. Installs on iPhone from Safari.",
  "scripts": {
    "start": "npx --yes http-server -c-1 -p 8080 .",
    "test": "node --test tests/*.test.js",
    "build": "node tools/build-sw.js",
    "icons": "NODE_PATH=$(npm root -g) node tools/make-icons.js",
    "e2e": "NODE_PATH=$(npm root -g) node tools/e2e.js"
  }
}
